# Claude Code local config
.claude/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist-electron
coverage
*.local
RELEASE*.md

# External service repository (managed separately)
services/netduo-wan-probe/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# SQLite persistence — user data and test fixtures should never land
# in the repo. Dev runs and vitest fixtures can produce these under the
# project directory.
*.sqlite
*.sqlite-journal
*.db
*.db-wal
*.db-shm

# Audit / remediation artifacts generated during v1.3 QA. Kept locally
# for reference but not part of the published repository.
AUDIT_REPORT.md
AUDIT_NOTES/
audit/

# Internal planning / scratch docs. Only the official /docs files that
# predate v1.3 (privacy.md, _config.yml, images/) are part of the repo.
docs/PLAN-*.md
docs/PLAN-*.txt

# Local AI assistant project guides. These configure how Claude Code,
# Codex CLI, Cursor and other agentic tools behave inside this repo
# (build-toolchain stability rules, file conventions, etc.). Kept
# local because they reflect the maintainer's workflow choices, not
# something every clone of the repo should inherit.
CLAUDE.md
AGENTS.md
.cursorrules

# Electron / build cache
*.tsbuildinfo
